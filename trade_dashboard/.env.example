# Optional alternative to .streamlit/secrets.toml. Copy to `.env` (which is
# git-ignored) and `source .env` before running streamlit.
DB_USER=class_user
DB_PASS='CLOU_!@#'
DB_HOST=35.254.120.209
DB_NAME=comtrade
DB_PORT=3306
